export type SaveUserEquipmentsDTO = {
  UserId: number;
  StationId: number;
  PluviometerId: number;
};

export type UpdateUserEquipmentsDTO = {
  UserId: number;
  StationId: number;
  PluviometerId: number;
};

{
  "name": "backend",
  "version": "1.0.0",
  "description": "Seai backend",
  "main": "dist/index.js",
  "author": "",
  "scripts": {
    "clean": "rm -rf ./dist",
    "test": "jest",
    "lint": "tslint --project tsconfig.json",
    "build": "tsc",
    "build:debug": "npm run clean && npm run build -- --sourceMap",
    "build:clean": "npm run clean && npm run build",
    "build:watch": "tsc -w",
    "build:start": "npm run build:clean && npm start",
    "start": "node dist/index.js",
    "start:debug": "concurrently -s all \"tsc -w\" nodemon dist/index.js\"",
    "start:dev": "nodemon"
  },
  "keywords": [],
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.2.5",
    "concurrently": "^8.2.0",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "tslint": "^6.1.3",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}

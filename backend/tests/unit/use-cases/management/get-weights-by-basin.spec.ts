describe("Management weights", () => {
  test.todo(
    "Should be to calculate 'water cut' and average culture indicators"
  );
  test.todo(
    "Should be to calculate weights when data provided by the repository is 'null'"
  );
});

describe("Management Crop", () => {
  test("shouldn't be able to create management crop without crop cycles", () => {
    /**
     *  DISCARD ETR, ETP, ETO, ETP_CYCLE from Crop
     */
  });

  test.todo("should be able to create management crop with crop cycles");
});
